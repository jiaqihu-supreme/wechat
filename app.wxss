@import "./styles/iconfont.wxss";

page,view,text,swiper,swiper-item,image,navigator {
  padding:0;
  margin:0;
  box-sizing:border-box;
}
/* 主题颜色 通过变量来实现
   less中存在变量这个知识
   原生css和wxss也是支持变量
*/
page {
  /* 定义主题颜色 */
  --themeColor:#9370DB;
  /* 定义统一的字体大小 假设设计稿 大小是375px*/
  font-size: 32rpx;

}
